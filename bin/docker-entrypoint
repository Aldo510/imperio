#!/bin/bash -e

# If running the web app, prepare the database before booting.
if { [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ]; } || \
   { [ "${1}" == "bundle" ] && [ "${2}" == "exec" ] && [ "${3}" == "puma" ]; }; then
  ./bin/rails db:prepare
fi

exec "${@}"
