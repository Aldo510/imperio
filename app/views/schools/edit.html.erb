<div class="mx-auto md:w-2/3 w-full mt-10">
    <h1 class="text-3xl dark:text-gray-100 font-bold mb-4"><%= @school.new_record? ? 'Nueva Escuela' : 'Editar Escuela' %></h1>

    <%= render 'form', school: @school %>

    <%= link_to 'Volver', schools_path, class: "mt-4 inline-block px-4 py-2 bg-gray-300 rounded hover:bg-gray-400" %>
</div>