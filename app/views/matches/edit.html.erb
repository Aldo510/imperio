<div class="container mx-auto py-8">
  <div class="max-w-lg mx-auto bg-white shadow-md rounded-lg p-6">
    <h2 class="text-2xl font-bold text-gray-800 mb-4 text-center">Registrar resultado</h2>
    
    <h3 class="text-xl font-semibold text-gray-700 mb-6 text-center"><%= @match.home_team.name %> vs <%= @match.away_team.name %></h3>

    <%= form_with model: @match, local: true, class: "space-y-4" do |form| %>
      <div>
        <%= form.label :home_score, 'Goles del equipo local', class: 'block text-gray-700 text-sm font-bold mb-2' %>
        <%= form.number_field :home_score, class: 'block w-full border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500' %>
      </div>

      <div>
        <%= form.label :away_score, 'Goles del equipo visitante', class: 'block text-gray-700 text-sm font-bold mb-2' %>
        <%= form.number_field :away_score, class: 'block w-full border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500' %>
      </div>

      <div class="text-center">
        <%= form.submit 'Guardar resultado', class: 'bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded' %>
      </div>
    <% end %>
  </div>
</div>
